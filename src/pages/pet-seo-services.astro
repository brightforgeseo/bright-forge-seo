---
import MainLayout from '../layouts/MainLayout.astro';
import LegacyHeader from '../components/LegacyHeader.astro';
import Footer from '../components/Footer.astro';
import ContactSection from '../components/ContactSection.astro';
import Hero from '../components/Hero.astro';
import ButtonPrimary from '../components/ButtonPrimary.astro';
import ButtonSecondary from '../components/ButtonSecondary.astro';
---

<MainLayout title="Pet SEO Services - Grow Your Pet Business Online | Bright Forge SEO" description="Specialized SEO services for pet businesses, veterinarians, pet stores, and animal services. Drive more pet owners to your business with expert pet industry SEO.">
  <LegacyHeader slot="header" />

  <!-- Hero Section -->
  <Hero
    badgeIcon="fas fa-paw"
    badgeText="Pet Industry SEO Experts"
    title="Pet SEO Services That Help Your Business Fetch More Customers"
    breadcrumbCurrent="Pet SEO Services"
  >
    <Fragment slot="story">
      <p>Ready to help more pet owners discover your business? At Bright Forge <a href="/">SEO Agency</a>, we understand the unique needs of pet businesses. From veterinary clinics and pet stores to grooming services and pet training, we craft specialized SEO strategies that connect you with pet owners actively searching for your services in your local area and beyond.</p>
    </Fragment>

    <Fragment slot="actions">
      <ButtonPrimary href="/contact" text="Start Your Pet SEO Journey" />
      <ButtonSecondary href="#services" text="Explore Our Services" />
    </Fragment>

    <Fragment slot="visual">
      <div class="hero-image-container">
        <div class="pet-icon-grid">
          <div class="pet-icon"><i class="fas fa-paw"></i></div>
          <div class="pet-icon"><i class="fas fa-heart"></i></div>
          <div class="pet-icon"><i class="fas fa-stethoscope"></i></div>
          <div class="pet-icon"><i class="fas fa-home"></i></div>
          <div class="pet-icon"><i class="fas fa-chart-line"></i></div>
          <div class="pet-icon"><i class="fas fa-star"></i></div>
        </div>
      </div>
    </Fragment>
  </Hero>

  <!-- Why Pet SEO Section -->
  <section class="why-pet-section">
    <div class="max-w-7xl mx-auto px-4">
      <div class="section-header">
        <h2>Why Pet Businesses Need <span>Specialized SEO</span></h2>
      </div>
      
      <div class="pet-explanation">
        <div class="pet-card">
          <div class="pet-icon">
            <i class="fas fa-heart"></i>
          </div>
          <div class="pet-text">
            <p>The pet industry is driven by emotion and trust. Pet owners don't just search for services—they search for caring professionals who will treat their beloved companions with love and expertise. Generic SEO strategies miss the emotional connection that drives pet owner decisions.</p>
            
            <p>Our pet industry SEO expertise helps you connect with pet owners when they need you most. Whether they're searching for emergency vet care, a trusted groomer, or the perfect pet supplies, we ensure your business appears at the top of search results with messaging that resonates with pet lovers.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Key Features Section -->
  <section id="services" class="features-section">
    <div class="max-w-7xl mx-auto px-4">
      <div class="section-header">
        <h2>What Makes Our Pet <span>SEO Services</span> Pawsome</h2>
        <p>We combine deep pet industry knowledge with proven SEO strategies to help your business thrive</p>
      </div>

      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-paw"></i>
          </div>
          <h3>Pet Industry Expertise</h3>
          <p>We understand pet owner behavior, seasonal trends, and the emotional factors that drive pet-related searches. Our team knows the difference between emergency and routine pet care searches, helping you capture the right traffic.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-map-marker-alt"></i>
          </div>
          <h3>Local Pet SEO Mastery</h3>
          <p>Most pet services are local businesses. We excel at local SEO optimization, ensuring your veterinary clinic, pet store, or grooming service appears when pet owners search in your area.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-calendar-alt"></i>
          </div>
          <h3>Seasonal Campaign Optimization</h3>
          <p>From holiday pet care to summer grooming peaks, we understand pet industry seasonality and optimize your campaigns to capture high-demand periods throughout the year.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-users"></i>
          </div>
          <h3>Trust-Building Content</h3>
          <p>Pet owners need to trust you with their family members. We create content that showcases your expertise, compassion, and commitment to animal welfare, building the trust that converts visitors into clients.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-mobile-alt"></i>
          </div>
          <h3>Mobile-First Approach</h3>
          <p>Pet emergencies happen anywhere, anytime. We ensure your website performs perfectly on mobile devices, making it easy for pet owners to find and contact you in urgent situations.</p>
        </div>
      </div>
    </div>
  </section>

  <ContactSection />
  <Footer slot="footer" />
</MainLayout>

<style>
  :root {
    --brand-primary: #1a1a1a;
    --brand-accent: #f97316;
    --brand-secondary: #2563eb;
    --brand-dark: #1f2937;
    --brand-soft: #6b7280;
    --brand-light: #f8fafc;
    --brand-white: #ffffff;
  }

  /* Pet Icon Grid (used in Hero visual slot) */
  .hero-image-container {
    position: relative;
    width: 100%;
    max-width: 400px;
  }

  .pet-icon-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
    padding: 2rem;
  }

  .pet-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 80px;
    height: 80px;
    background: rgba(255, 255, 255, 0.15);
    border-radius: 20px;
    backdrop-filter: blur(10px);
    color: white;
    font-size: 1.8rem;
    transition: all 0.3s ease;
    animation: iconFloat 4s ease-in-out infinite;
  }

  .pet-icon:nth-child(1) { animation-delay: 0s; }
  .pet-icon:nth-child(2) { animation-delay: 0.5s; }
  .pet-icon:nth-child(3) { animation-delay: 1s; }
  .pet-icon:nth-child(4) { animation-delay: 1.5s; }
  .pet-icon:nth-child(5) { animation-delay: 2s; }
  .pet-icon:nth-child(6) { animation-delay: 2.5s; }

  @keyframes iconFloat {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
  }

  .pet-icon:hover {
    transform: scale(1.1) translateY(-5px);
    background: rgba(255, 255, 255, 0.25);
  }

  /* Why Pet Section */
  .why-pet-section {
    padding: 80px 0;
    background: #f8fafc;
  }

  .pet-explanation {
    max-width: 4xl;
    margin: 0 auto;
  }

  .pet-card {
    display: flex;
    align-items: flex-start;
    gap: 2rem;
    background: white;
    padding: 3rem;
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
  }

  .pet-card .pet-icon {
    flex-shrink: 0;
    width: 80px;
    height: 80px;
    background: linear-gradient(135deg, #f97316, #FF6B35);
    border-radius: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 2rem;
  }

  .pet-text p {
    font-size: 1.1rem;
    line-height: 1.7;
    color: var(--brand-dark);
    margin-bottom: 1.5rem;
  }

  .pet-text p:last-child {
    margin-bottom: 0;
  }

  /* Features Section */
  .features-section {
    padding: 80px 0;
    background: white;
  }

  .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-header h2 {
    font-size: clamp(2rem, 3vw, 2.5rem);
    font-weight: 700;
    color: var(--brand-dark);
    margin-bottom: 1rem;
  }

  .section-header h2 span {
    background: linear-gradient(135deg, #f97316, #4285F4);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .section-header p {
    font-size: 1.2rem;
    color: var(--brand-soft);
    max-width: 600px;
    margin: 0 auto;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .feature-card {
    position: relative;
    background: white;
    padding: 2.5rem;
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
    transition: all 0.4s ease;
    border: 2px solid #f0f0f0;
    overflow: hidden;
  }

  .feature-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 5px;
    background: linear-gradient(135deg, #f97316, #FF6B35);
    opacity: 0;
    transition: opacity 0.4s ease;
  }

  .feature-card:hover::before {
    opacity: 1;
  }

  .feature-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 15px 40px rgba(249, 115, 22, 0.15);
    border-color: rgba(249, 115, 22, 0.3);
  }

  .feature-icon {
    width: 90px;
    height: 90px;
    background: linear-gradient(135deg, #f97316, #FF6B35);
    border-radius: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 2.25rem;
    margin-bottom: 1.5rem;
    box-shadow: 0 8px 20px rgba(249, 115, 22, 0.25);
    transition: all 0.4s ease;
  }

  .feature-card:hover .feature-icon {
    transform: scale(1.1) rotate(5deg);
    box-shadow: 0 12px 30px rgba(249, 115, 22, 0.35);
  }

  .feature-card h3 {
    font-size: 1.4rem;
    font-weight: 600;
    color: var(--brand-dark);
    margin-bottom: 1rem;
  }

  .feature-card p {
    color: var(--brand-soft);
    line-height: 1.6;
  }

  /* Mobile Responsive */
  @media (max-width: 768px) {
    .pet-icon-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 1rem;
      padding: 1rem;
    }

    .pet-icon {
      width: 60px;
      height: 60px;
      font-size: 1.4rem;
    }

    .pet-card {
      flex-direction: column;
      text-align: center;
      padding: 2rem;
    }

    .features-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    window.toggleFAQ = function(element) {
      const answer = element.nextElementSibling;
      const icon = element.querySelector('i');
      
      if (answer.style.display === 'block') {
        answer.style.display = 'none';
        icon.classList.remove('fa-minus');
        icon.classList.add('fa-plus');
      } else {
        answer.style.display = 'block';
        icon.classList.remove('fa-plus');
        icon.classList.add('fa-minus');
      }
    };
  });
</script>
