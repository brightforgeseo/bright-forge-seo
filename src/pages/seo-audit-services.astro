---
import MainLayout from '../layouts/MainLayout.astro';
import LegacyHeader from '../components/LegacyHeader.astro';
import Footer from '../components/Footer.astro';
import ContactSection from '../components/ContactSection.astro';
---

<MainLayout title="Comprehensive SEO Audit Services Philippines | Bright Forge SEO" description="Pinpoint technical issues, content gaps, and backlink weaknesses with our in-depth SEO audits. Get a clear roadmap to higher rankings and conversions.">
  <LegacyHeader slot="header" />

  <!-- Hero Section -->
  <section class="hero-seo-audit">
    <!-- Animated Background Elements -->
    <div class="hero-bg-elements">
      <div class="floating-circle circle-1"></div>
      <div class="floating-circle circle-2"></div>
      <div class="floating-circle circle-3"></div>
    </div>
    
    <div class="max-w-7xl mx-auto px-4">
      <!-- Breadcrumbs -->
      <nav class="hero-breadcrumbs">
        <a href="/" class="breadcrumb-link">Home</a>
        <span class="breadcrumb-separator">›</span>
        <span class="breadcrumb-current">SEO Audit Services</span>
      </nav>
      
      <div class="hero-container">
        <div class="hero-content">
          <div class="hero-badge">
            <i class="fas fa-search-plus"></i>
            <span>SEO Audit Specialists</span>
          </div>
          
          <h1>Comprehensive SEO Audit Services</h1>
          
          <div class="hero-story">
            <p>Wondering why your traffic plateaued - or worse, dropped overnight? Let's shine a light on the hidden roadblocks.</p>
            
            <p>Our Philippines-based SEO detectives crawl every nook and cranny of your website - technical code, content, links, you name it. Then we translate all the jargon into a plain-English game plan you can actually act on.</p>
          </div>
          
          <div class="hero-actions">
            <a href="/contact" class="cta-button primary">
              <span>Get Your Free SEO Audit</span>
              <i class="fas fa-arrow-right"></i>
            </a>
            <a href="#deliverables" class="cta-button secondary">
              <i class="fas fa-file-alt"></i>
              <span>See Sample Report</span>
            </a>
          </div>

        </div>
        
        <div class="hero-visual">
          <div class="hero-image-container">
            <div class="audit-icon-grid">
              <div class="audit-icon"><i class="fas fa-chart-line"></i></div>
              <div class="audit-icon"><i class="fas fa-cogs"></i></div>
              <div class="audit-icon"><i class="fas fa-link"></i></div>
              <div class="audit-icon"><i class="fas fa-edit"></i></div>
              <div class="audit-icon"><i class="fas fa-mobile-alt"></i></div>
              <div class="audit-icon"><i class="fas fa-tachometer-alt"></i></div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Free SEO Audit Tool -->
  <section class="py-40 bg-white">
    <div class="max-w-6xl mx-auto px-8 text-center">
      
      <!-- Main Heading -->
      <div class="mb-32">
        <h1 class="text-7xl font-black text-gray-900 mb-8 leading-tight">
          Free SEO Audit Tool
        </h1>
        <div class="w-40 h-1.5 bg-[#FF4438] mx-auto mb-12"></div>
        <p class="text-2xl text-gray-600 max-w-4xl mx-auto leading-relaxed">
          Get a comprehensive analysis of your website's SEO performance and discover what's holding you back from ranking higher in search results
        </p>
      </div>

      <!-- Main Form -->
      <div class="max-w-3xl mx-auto mb-48">
        <form name="seo-audit-tool" class="flex border-2 border-gray-900 rounded-none overflow-hidden">
          <input 
            type="url" 
            id="audit-website-url" 
            name="website-url" 
            placeholder="Example.com" 
            required 
            class="flex-1 px-8 py-6 text-xl border-none focus:outline-none bg-white"
          />
          <button 
            type="submit" 
            class="bg-[#FF4438] hover:bg-[#FF9500] text-white font-bold px-12 py-6 text-xl transition-colors"
          >
            Audit
          </button>
        </form>
        
        <p class="text-gray-600 mt-8 text-xl">
          Enter your website URL and get a free comprehensive SEO analysis in seconds
        </p>

        <!-- Loading State -->
        <div id="loading-state" class="hidden text-center py-12">
          <div class="inline-flex items-center gap-4">
            <div class="w-8 h-8 border-2 border-[#FF4438] border-t-transparent rounded-full animate-spin"></div>
            <span class="text-gray-600 text-lg">Analyzing your website...</span>
          </div>
        </div>

        <!-- Results Container -->
        <div id="audit-results" class="hidden mt-12"></div>
      </div>
    </div>
  </section>

  <!-- Why Audit Section -->
  <section class="why-audit-section">
    <div class="max-w-7xl mx-auto px-4">
      <div class="section-header">
        <div class="section-badge">Why Audit?</div>
        <h2>Why Your Site Needs an <span>SEO Audit</span></h2>
        <p>Uncover the hidden issues that are silently sabotaging your search rankings and user experience.</p>
      </div>
      
      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon reveal">
            <i class="fas fa-search"></i>
          </div>
          <h3>Reveal Hidden Issues</h3>
          <p>Broken links, duplicate content, slow pages - uncover problems you didn't know existed.</p>
          <div class="feature-metric">
            <span class="metric-value">83%</span>
            <span class="metric-label">Sites Have Hidden Issues</span>
        </div>
        </div>
        
        <div class="feature-card">
          <div class="feature-icon lightbulb">
            <i class="fas fa-lightbulb"></i>
          </div>
          <h3>Unlock New Opportunities</h3>
          <p>Spot untapped keywords and content gaps that can boost your visibility fast.</p>
          <div class="feature-metric">
            <span class="metric-value">127%</span>
            <span class="metric-label">Average Traffic Boost</span>
        </div>
        </div>
        
        <div class="feature-card">
          <div class="feature-icon roadmap">
            <i class="fas fa-road"></i>
          </div>
          <h3>Actionable Roadmap</h3>
          <p>We don't just list issues - we give you step-by-step fixes prioritized for impact.</p>
          <div class="feature-metric">
            <span class="metric-value">30+</span>
            <span class="metric-label">Actionable Recommendations</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Signs You Need An Audit -->
  <section class="signs-section">
    <div class="max-w-7xl mx-auto px-4">
      <div class="section-header">
        <div class="section-badge">Warning Signs</div>
        <h2>Signs It's Time for an <span>SEO Check-Up</span></h2>
        <p>Recognize these red flags? Your website is crying out for professional attention.</p>
      </div>
      
      <div class="signs-grid">
        <div class="sign-card urgent">
          <div class="sign-icon">📉</div>
          <h3>Traffic Dropping</h3>
          <p>Sudden drop in organic traffic without obvious cause</p>
        </div>
        
        <div class="sign-card warning">
          <div class="sign-icon">🐌</div>
          <h3>Slow Loading</h3>
          <p>Pages taking ages to load, especially on mobile devices</p>
        </div>
        
        <div class="sign-card attention">
          <div class="sign-icon">📄</div>
          <h3>Poor Rankings</h3>
          <p>Keywords stuck on page 2+ despite optimization efforts</p>
        </div>
        
        <div class="sign-card warning">
          <div class="sign-icon">📱</div>
          <h3>Mobile Issues</h3>
          <p>Mobile users bouncing faster than desktop visitors</p>
        </div>
        
        <div class="sign-card urgent">
          <div class="sign-icon">❌</div>
          <h3>Console Errors</h3>
          <p>Technical errors piling up in Google Search Console</p>
        </div>
        
        <div class="sign-card attention">
          <div class="sign-icon">🔍</div>
          <h3>Low Visibility</h3>
          <p>Not showing up for branded searches or key terms</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Audit Process Section -->
  <section id="process" class="process-section">
    <div class="max-w-7xl mx-auto px-4">
      <div class="section-header">
        <div class="section-badge">Our Process</div>
        <h2>Our Proven <span>Audit Process</span></h2>
        <p>A systematic approach that leaves no stone unturned in diagnosing your SEO challenges.</p>
      </div>

      <div class="process-timeline">
        <div class="timeline-line"></div>
        
        <div class="step-card">
          <div class="step-number">1</div>
          <div class="step-content">
          <h3>Crawl & Index Analysis</h3>
          <p>We run deep crawls to understand how search engines view your site.</p>
            <div class="step-details">
              <div class="detail-item">🕷️ Site Crawling</div>
              <div class="detail-item">📋 Index Status Check</div>
              <div class="detail-item">🔍 URL Structure Analysis</div>
        </div>
          </div>
          <div class="step-visual">
            <div class="visual-icon">🕸️</div>
          </div>
        </div>

        <div class="step-card reverse">
          <div class="step-number">2</div>
          <div class="step-content">
          <h3><a href="/technical-seo-services-philippines">Technical Diagnostics</a></h3>
          <p>Identify speed bottlenecks, security issues, and mobile UX flaws.</p>
            <div class="step-details">
              <div class="detail-item">⚡ Speed Testing</div>
              <div class="detail-item">🔒 Security Scan</div>
              <div class="detail-item">📱 Mobile Analysis</div>
        </div>
          </div>
          <div class="step-visual">
            <div class="visual-icon">🔧</div>
          </div>
        </div>

        <div class="step-card">
          <div class="step-number">3</div>
          <div class="step-content">
          <h3><a href="/on-page-seo-services-philippines">On-Page & Content Review</a></h3>
          <p>Assess keyword usage, structure, and content depth for every key page.</p>
            <div class="step-details">
              <div class="detail-item">📝 Content Audit</div>
              <div class="detail-item">🏷️ Meta Tag Review</div>
              <div class="detail-item">🔗 Internal Link Analysis</div>
        </div>
          </div>
          <div class="step-visual">
            <div class="visual-icon">📄</div>
          </div>
        </div>

        <div class="step-card reverse">
          <div class="step-number">4</div>
          <div class="step-content">
          <h3><a href="/backlink-seo-services-philippines">Backlink & Competitor Benchmark</a></h3>
          <p>Compare your link profile and authority against top competitors.</p>
            <div class="step-details">
              <div class="detail-item">🔗 Link Profile Analysis</div>
              <div class="detail-item">🏆 Competitor Research</div>
              <div class="detail-item">📊 Authority Assessment</div>
        </div>
          </div>
          <div class="step-visual">
            <div class="visual-icon">🔗</div>
          </div>
        </div>

        <div class="step-card">
          <div class="step-number">5</div>
          <div class="step-content">
          <h3>Actionable Report & Roadmap</h3>
          <p>Deliver a prioritized task list complete with quick wins and long-term fixes.</p>
            <div class="step-details">
              <div class="detail-item">📊 Comprehensive Report</div>
              <div class="detail-item">🎯 Priority Roadmap</div>
              <div class="detail-item">🎥 Video Walkthrough</div>
            </div>
          </div>
          <div class="step-visual">
            <div class="visual-icon">📋</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Deliverables Section -->
  <section id="deliverables" class="deliverables-section">
    <div class="max-w-7xl mx-auto px-4">
      <div class="section-header">
        <div class="section-badge">What You Get</div>
        <h2>What You'll <span>Receive</span></h2>
        <p>A comprehensive audit package designed to give you complete clarity and actionable next steps.</p>
      </div>

      <div class="feature-list">
        <div class="feature-item">
          <div class="feature-icon-large">
            <i class="fas fa-file-pdf"></i>
          </div>
          <div class="feature-content">
            <h3>Comprehensive PDF Report</h3>
            <p>30+ pages detailing every issue & opportunity, ranked by priority with clear explanations and fix recommendations.</p>
            <div class="feature-benefit">📄 Detailed 30-page analysis</div>
          </div>
        </div>
        
        <div class="feature-item">
          <div class="feature-icon-large">
            <i class="fas fa-video"></i>
          </div>
          <div class="feature-content">
            <h3>Personal Video Walkthrough</h3>
            <p>We record a screen-share video explaining the findings in human terms, so you understand exactly what needs fixing.</p>
            <div class="feature-benefit">🎥 15-minute walkthrough</div>
          </div>
        </div>
        
        <div class="feature-item">
          <div class="feature-icon-large">
            <i class="fas fa-tasks"></i>
          </div>
          <div class="feature-content">
            <h3>Implementation Spreadsheet</h3>
            <p>Ready-to-import task list for your dev or content team with priorities, timelines, and success metrics.</p>
            <div class="feature-benefit">📋 50+ actionable tasks</div>
          </div>
        </div>
        
        <div class="feature-item">
          <div class="feature-icon-large">
            <i class="fas fa-phone"></i>
          </div>
          <div class="feature-content">
            <h3>Strategy Consultation Call</h3>
            <p>60-minute follow-up call to discuss findings, answer questions, and create your custom implementation plan.</p>
            <div class="feature-benefit">📞 1-hour strategy session</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ Section -->
  <section class="faq-section">
    <div class="max-w-4xl mx-auto px-4">
      <div class="section-header text-center">
        <div class="section-badge">FAQ</div>
        <h2>Frequently Asked <span>Questions</span></h2>
        <p>Everything you need to know about our comprehensive SEO audit process.</p>
      </div>
      
      <div class="faq-list">
        <div class="faq-item">
          <button class="faq-question" onclick="toggleFAQ(this)">
            <span>How long does an SEO audit take?</span>
            <i class="fas fa-plus"></i>
          </button>
          <div class="faq-answer">Most audits are completed within 7-10 business days, depending on site size and complexity. Large e-commerce sites may take up to 14 days, while smaller business sites typically complete in 5-7 days. We'll provide an accurate timeline after reviewing your site.</div>
        </div>
        
        <div class="faq-item">
          <button class="faq-question" onclick="toggleFAQ(this)">
            <span>Will you implement the fixes for me?</span>
            <i class="fas fa-plus"></i>
          </button>
          <div class="faq-answer">Yes! We offer full implementation packages or can coach your in-house team - whichever suits your needs. Our implementation services start at ₱50,000 per month and include ongoing monitoring and optimization.</div>
        </div>
        
        <div class="faq-item">
          <button class="faq-question" onclick="toggleFAQ(this)">
            <span>Is the audit compliant with Google's latest guidelines?</span>
            <i class="fas fa-plus"></i>
          </button>
          <div class="faq-answer">Absolutely. Our methodology aligns with the latest Google Search Essentials and Core Update recommendations. We stay current with algorithm changes and update our audit process accordingly to ensure relevance and accuracy.</div>
        </div>
        
        <div class="faq-item">
          <button class="faq-question" onclick="toggleFAQ(this)">
            <span>What access do you need?</span>
            <i class="fas fa-plus"></i>
          </button>
          <div class="faq-answer">Typically, read-only access to Google Analytics, Search Console, and your CMS. We'll guide you through secure provisioning. We never need admin access and can work with view-only permissions to protect your site security.</div>
        </div>
        
        <div class="faq-item">
          <button class="faq-question" onclick="toggleFAQ(this)">
            <span>How much does an SEO audit cost?</span>
            <i class="fas fa-plus"></i>
          </button>
          <div class="faq-answer">Our comprehensive SEO audits start at ₱25,000 (~$450) for small business sites and scale based on complexity. Enterprise sites with 1000+ pages start at ₱75,000 (~$1,350). Contact us for a custom quote based on your specific needs.</div>
        </div>
        
        <div class="faq-item">
          <button class="faq-question" onclick="toggleFAQ(this)">
            <span>Do you provide ongoing support after the audit?</span>
            <i class="fas fa-plus"></i>
          </button>
          <div class="faq-answer">Yes! Every audit includes 30 days of email support to answer questions about implementation. We also offer monthly SEO maintenance packages starting at ₱30,000/month for ongoing optimization and monitoring.</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Contact Section -->
  <ContactSection id="contact" />
  <Footer slot="footer" />
</MainLayout>

<style>
/* Enhanced SEO Audit Hero Section Styles */
.hero-seo-audit {
  position: relative;
  min-height: 100vh;
  display: flex;
  align-items: center;
  background: linear-gradient(135deg, #FF4438 0%, #FF6B35 50%, #4285F4 100%);
  background-size: 400% 400%;
  animation: gradientShift 15s ease infinite;
  overflow: hidden;
  padding: 2rem 0;
}

@keyframes gradientShift {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

/* Background Elements */
.hero-bg-elements {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
  z-index: 1;
}

.floating-circle {
  position: absolute;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(20px);
  animation: float 6s ease-in-out infinite;
}

.circle-1 {
  width: 120px;
  height: 120px;
  top: 20%;
  left: 10%;
  animation-delay: 0s;
}

.circle-2 {
  width: 80px;
  height: 80px;
  top: 60%;
  right: 15%;
  animation-delay: 2s;
}

.circle-3 {
  width: 100px;
  height: 100px;
  bottom: 30%;
  left: 20%;
  animation-delay: 4s;
}

.circle-4 {
  width: 60px;
  height: 60px;
  top: 10%;
  right: 30%;
  animation-delay: 1s;
}

@keyframes float {
  0%, 100% { transform: translateY(0px) rotate(0deg); }
  33% { transform: translateY(-20px) rotate(120deg); }
  66% { transform: translateY(10px) rotate(240deg); }
}

/* Breadcrumbs */
.hero-breadcrumbs {
  display: inline-block !important;
  margin-bottom: 3rem;
  font-size: 14px;
  font-weight: 500;
  line-height: 1;
  position: relative;
  z-index: 10;
}

.hero-breadcrumbs * {
  display: inline !important;
  vertical-align: baseline !important;
}

.breadcrumb-link {
  color: white !important;
  text-decoration: none;
  transition: all 0.3s ease;
  opacity: 0.9;
}

.breadcrumb-link:hover {
  opacity: 1;
  text-decoration: underline;
}

.breadcrumb-separator {
  color: white !important;
  margin: 0 8px;
  opacity: 0.7;
}

.breadcrumb-current {
  color: white !important;
  font-weight: 600;
}

/* Hero Container */
.hero-container {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
  align-items: center;
  position: relative;
  z-index: 10;
}

/* Hero Content */
.hero-content {
  color: white;
}

.hero-badge {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  background: rgba(255, 255, 255, 0.15);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.2);
  padding: 0.5rem 1rem;
  border-radius: 50px;
  font-size: 0.875rem;
  font-weight: 500;
  margin-bottom: 1.5rem;
  animation: fadeInUp 0.8s ease-out 0.2s both;
}

.hero-badge i {
  font-size: 1rem;
  color: white;
}

.hero-content h1 {
  font-size: clamp(2.5rem, 5vw, 4rem);
  font-weight: 800;
  line-height: 1.1;
  margin-bottom: 1.5rem;
  animation: fadeInUp 0.8s ease-out 0.4s both;
}

.hero-subtitle {
  font-size: 1.25rem;
  font-weight: 600;
  margin-bottom: 1.5rem;
  opacity: 0.95;
  animation: fadeInUp 0.8s ease-out 0.6s both;
}

.hero-content p {
  font-size: 1.1rem;
  line-height: 1.7;
  margin-bottom: 1rem;
  opacity: 0.9;
  animation: fadeInUp 0.8s ease-out 0.8s both;
}

/* Hero Stats */
.hero-stats {
  display: flex;
  gap: 2rem;
  margin: 2rem 0;
  animation: fadeInUp 0.8s ease-out 1s both;
}

.stat-item {
  text-align: center;
}

.stat-number {
  display: block;
  font-size: 2rem;
  font-weight: 800;
  color: white;
  margin-bottom: 0.25rem;
}

.stat-label {
  font-size: 0.875rem;
  opacity: 0.8;
  font-weight: 500;
}

/* CTA Buttons */
.hero-ctas {
  display: flex;
  gap: 1rem;
  margin-top: 2rem;
  animation: fadeInUp 0.8s ease-out 1.2s both;
}

.cta-primary, .cta-secondary {
  display: inline-flex;
  align-items: center;
  padding: 1rem 2rem;
  border-radius: 50px;
  font-weight: 600;
  text-decoration: none;
  transition: all 0.3s ease;
  font-size: 1rem;
}

.cta-primary {
  background: white;
  color: #FF4438;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
}

.cta-primary:hover {
  background: #f8f9fa;
  transform: translateY(-2px);
  box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
}

.cta-secondary {
  background: rgba(255, 255, 255, 0.15);
  color: white;
  border: 1px solid rgba(255, 255, 255, 0.3);
  backdrop-filter: blur(20px);
}

.cta-secondary:hover {
  background: rgba(255, 255, 255, 0.25);
  transform: translateY(-2px);
}

/* SEO Audit Icon Grid */
.audit-icon-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 1.5rem;
  animation: fadeInUp 0.8s ease-out 1.4s both;
}

.icon-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  padding: 1.5rem;
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 16px;
  color: white;
  transition: all 0.3s ease;
  animation: iconFloat 3s ease-in-out infinite;
}

.icon-item:nth-child(even) {
  animation-delay: 1s;
}

.icon-item:hover {
  transform: translateY(-5px);
  background: rgba(255, 255, 255, 0.15);
  border-color: rgba(255, 255, 255, 0.3);
}

.icon-item i {
  font-size: 2rem;
  margin-bottom: 0.75rem;
  color: white;
}

.icon-item span {
  font-size: 0.875rem;
  font-weight: 500;
  opacity: 0.9;
}

@keyframes iconFloat {
  0%, 100% { transform: translateY(0px); }
  50% { transform: translateY(-10px); }
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* Responsive Design */
@media (max-width: 768px) {
  .hero-container {
    grid-template-columns: 1fr;
    gap: 2rem;
    text-align: center;
  }
  
  .hero-stats {
    justify-content: center;
    gap: 1.5rem;
  }
  
  .hero-ctas {
    flex-direction: column;
    align-items: center;
  }
  
  .audit-icon-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem;
  }
  
  .icon-item {
    padding: 1rem;
  }
  
  .icon-item i {
    font-size: 1.5rem;
  }
  
  .cta-primary, .cta-secondary {
    padding: 0.875rem 1.5rem;
    font-size: 0.9rem;
  }
}

/********************
  CSS Variables & Reset
*********************/
:root {
  --brand-accent: #FF4438;
  --brand-blue: #4285F4;
  --brand-dark: #1E2022;
  --brand-soft: #52575D;
  --brand-light: #F8F9FA;
  --gradient-primary: linear-gradient(135deg, #4285F4 0%, #667eea 100%);
  --gradient-secondary: linear-gradient(135deg, #FF4438 0%, #FF6B35 100%);
  --gradient-accent: linear-gradient(135deg, #4285F4 0%, #FF4438 100%);
  --shadow-soft: 0 4px 20px rgba(0, 0, 0, 0.08);
  --shadow-medium: 0 8px 30px rgba(0, 0, 0, 0.12);
  --shadow-strong: 0 15px 50px rgba(0, 0, 0, 0.15);
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Inter', 'DM Sans', sans-serif;
  line-height: 1.6;
  color: var(--brand-dark);
  overflow-x: hidden;
}

/* Page centering fix */
.container,
.max-w-7xl,
.max-w-4xl {
  max-width: 80rem;
  width: 100%;
  margin-left: auto !important;
  margin-right: auto !important;
  padding-left: 1rem;
  padding-right: 1rem;
}

.max-w-4xl {
  max-width: 56rem;
}

@media (min-width: 640px) {
  .container,
  .max-w-7xl,
  .max-w-4xl {
    padding-left: 1.5rem;
    padding-right: 1.5rem;
  }
}

@media (min-width: 1024px) {
  .container,
  .max-w-7xl,
  .max-w-4xl {
    padding-left: 2rem;
    padding-right: 2rem;
  }
}

/********************
  Section Styling
*********************/
section {
  padding: 5rem 0;
}

.section-header {
  text-align: center;
  margin-bottom: 4rem;
  max-width: 800px;
  margin-left: auto;
  margin-right: auto;
}

.section-badge {
  display: inline-block;
  background: var(--gradient-accent);
  color: white;
  padding: 8px 20px;
  border-radius: 50px;
  font-size: 14px;
  font-weight: 600;
  margin-bottom: 1rem;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.section-header h2 {
  font-size: clamp(2rem, 5vw, 3.5rem);
  font-weight: 700;
  line-height: 1.2;
  margin-bottom: 1rem;
}

.section-header span {
  background: var(--gradient-accent);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.section-header p {
  font-size: 1.2rem;
  color: var(--brand-soft);
  max-width: 600px;
  margin: 0 auto;
}

/********************
  Legacy Hero Styles Removed - Using Enhanced Hero Design
*********************/

/* Legacy styles removed - using enhanced hero design */

/* Legacy dashboard styles removed - using enhanced icon grid design */

/********************
  Social Proof Banner
*********************/
/* Removed */

/********************
  Why Audit Section
*********************/
.why-audit-section {
  background: white;
}

.features-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 2rem;
}

.feature-card {
  background: white;
  border-radius: 16px;
  padding: 2.5rem;
  text-align: center;
  box-shadow: var(--shadow-soft);
  border: 1px solid #f0f0f0;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}

.feature-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: var(--gradient-accent);
}

.feature-card:hover {
  transform: translateY(-8px);
  box-shadow: var(--shadow-medium);
}

.feature-icon {
  width: 80px;
  height: 80px;
  margin: 0 auto 1.5rem;
  border-radius: 20px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 2rem;
  color: white;
  position: relative;
}

.feature-icon.reveal {
  background: linear-gradient(135deg, #667eea, #764ba2);
}

.feature-icon.lightbulb {
  background: linear-gradient(135deg, #ffd700, #ffed4e);
}

.feature-icon.roadmap {
  background: linear-gradient(135deg, #00d084, #00a86b);
}

.feature-card h3 {
  font-size: 1.5rem;
  font-weight: 700;
  margin-bottom: 1rem;
  color: var(--brand-dark);
}

.feature-card p {
  color: var(--brand-soft);
  line-height: 1.6;
  margin-bottom: 1.5rem;
}

.feature-metric {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  background: var(--brand-light);
  padding: 12px 20px;
  border-radius: 50px;
  margin-top: 1rem;
}

.metric-value {
  font-size: 1.5rem;
  font-weight: 800;
  color: var(--brand-accent);
}

.metric-label {
  font-size: 0.9rem;
  color: var(--brand-soft);
}

/********************
  Coverage Section
*********************/
/* Removed */

/********************
  Signs Section
*********************/
.signs-section {
  background: white;
}

.signs-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 2rem;
}

.sign-card {
  background: white;
  border-radius: 16px;
  padding: 2rem;
  text-align: center;
  box-shadow: var(--shadow-soft);
  border: 1px solid #f0f0f0;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}

.sign-card:hover {
  transform: translateY(-5px);
  box-shadow: var(--shadow-medium);
}

.sign-card.urgent {
  border-left: 4px solid #ff4757;
}

.sign-card.warning {
  border-left: 4px solid #ffd700;
}

.sign-card.attention {
  border-left: 4px solid #4285F4;
}

.sign-icon {
  font-size: 3rem;
  margin-bottom: 1rem;
}

.sign-card h3 {
  font-size: 1.2rem;
  font-weight: 700;
  margin-bottom: 0.5rem;
  color: var(--brand-dark);
}

.sign-card p {
  color: var(--brand-soft);
  line-height: 1.6;
}

/********************
  Process Section
*********************/
.process-section {
  background: white;
}

.process-timeline {
  position: relative;
  max-width: 1000px;
  margin: 0 auto;
}

.timeline-line {
  position: absolute;
  left: 50%;
  top: 0;
  bottom: 0;
  width: 3px;
  background: linear-gradient(to bottom, var(--brand-blue), var(--brand-accent));
  transform: translateX(-50%);
  z-index: 1;
}

@media (max-width: 768px) {
  .timeline-line {
    left: 30px;
  }
}

.step-card {
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  gap: 2rem;
  align-items: center;
  margin-bottom: 4rem;
  position: relative;
  z-index: 2;
}

.step-card.reverse {
  grid-template-columns: 1fr auto 1fr;
}

.step-card.reverse .step-content {
  grid-column: 3;
  text-align: left;
}

.step-card.reverse .step-visual {
  grid-column: 1;
}

@media (max-width: 768px) {
  .step-card,
  .step-card.reverse {
    grid-template-columns: auto 1fr;
    gap: 1.5rem;
  }
  
  .step-card .step-content,
  .step-card.reverse .step-content {
    grid-column: 2;
    text-align: left;
  }
  
  .step-card .step-visual,
  .step-card.reverse .step-visual {
    grid-column: 2;
    grid-row: 1;
  }
}

.step-number {
  width: 60px;
  height: 60px;
  border-radius: 50%;
  background: var(--gradient-accent);
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 800;
  font-size: 1.5rem;
  box-shadow: var(--shadow-medium);
  position: relative;
  z-index: 3;
}

.step-content {
  background: white;
  padding: 2.5rem;
  border-radius: 16px;
  box-shadow: var(--shadow-soft);
  border: 1px solid #f0f0f0;
}

.step-content h3 {
  font-size: 1.3rem;
  font-weight: 700;
  color: var(--color-heading);
  margin-bottom: 0.5rem;
}

.step-content h3 a {
  color: var(--color-heading);
  text-decoration: none;
  transition: color 0.3s;
  position: relative;
}

.step-content h3 a:hover {
  color: var(--brand-accent);
}

.step-content h3 a::after {
  content: '';
  position: absolute;
  width: 100%;
  height: 2px;
  bottom: -2px;
  left: 0;
  background-color: var(--brand-accent);
  transform: scaleX(0);
  transform-origin: bottom right;
  transition: transform 0.3s;
}

.step-content h3 a:hover::after {
  transform: scaleX(1);
  transform-origin: bottom left;
}

.step-content p {
  color: var(--brand-soft);
  line-height: 1.6;
  margin-bottom: 1.5rem;
}

.step-details {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
}

.detail-item {
  background: var(--brand-light);
  padding: 6px 12px;
  border-radius: 20px;
  font-size: 0.9rem;
  color: var(--brand-soft);
}

.step-visual {
  width: 100px;
  height: 100px;
  background: var(--gradient-secondary);
  border-radius: 20px;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: var(--shadow-medium);
}

.visual-icon {
  font-size: 2.5rem;
}

/********************
  Deliverables Section
*********************/
.deliverables-section {
  background: var(--brand-light);
}

.feature-list {
  display: grid;
  gap: 2rem;
  max-width: 900px;
  margin: 0 auto;
}

.feature-item {
  display: grid;
  grid-template-columns: auto 1fr;
  gap: 2rem;
  align-items: flex-start;
}

.feature-item .feature-icon-large {
  width: 56px;
  height: 56px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border-radius: 14px;
  background: var(--gradient-accent);
  color: #fff;
  box-shadow: var(--shadow-soft);
  flex-shrink: 0;
}

.feature-item .feature-icon-large i {
  font-size: 22px;
  line-height: 1;
}

.feature-item .feature-content h3 {
  font-size: 1.125rem; /* 18px */
  font-weight: 700;
  margin-top: 4px;
  margin-bottom: 6px;
  color: var(--brand-dark);
}

.feature-item .feature-content p {
  color: var(--brand-soft);
  line-height: 1.65;
}

/* subtle meta/benefit line under each deliverable */
.feature-item .feature-benefit {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-top: 6px;
  color: #6b7280;
  font-size: 0.95rem;
}

/* Legacy responsive styles removed - using enhanced hero design */

/********************
  FAQ Section
*********************/
.faq-section {
  background: white;
}

.faq-list {
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.faq-item {
  background: #fff;
  border: 1px solid #e5e7eb;
  border-radius: 12px;
  box-shadow: var(--shadow-soft);
  overflow: hidden;
}

.faq-question {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
  text-align: left;
  background: #fff;
  border: 0;
  padding: 1.25rem 1.25rem;
  font-size: 1rem;
  font-weight: 600;
  color: #111827;
  cursor: pointer;
}

.faq-question i {
  transition: transform 0.2s ease;
  color: #6b7280;
}

.faq-answer {
  max-height: 0;
  overflow: hidden;
  transition: max-height 0.3s ease;
  padding: 0 1.25rem;
  color: #4b5563;
}

.faq-item.open .faq-answer {
  /* enough to fit a few paragraphs; JS will also set inline height for smoother animation */
  max-height: 600px;
  padding-bottom: 1.25rem;
}

.faq-item.open .faq-question i.fa-plus {
  /* swap handled in JS; keep a rotate fallback */
  transform: rotate(45deg);
}

/********************
  Mobile Responsive Styles
*********************/
@media (max-width: 768px) {
  section {
    padding: 3rem 0;
  }
  
  .section-grid {
    grid-template-columns: 1fr;
    gap: 24px;
  }
  
  .coverage-grid {
    grid-template-columns: 1fr;
  }
  
  .process-steps {
    grid-template-columns: 1fr;
  }
  
  .deliverables-grid {
    grid-template-columns: 1fr;
  }
  
  .features-grid {
    grid-template-columns: 1fr;
  }
  
  .coverage-grid {
    grid-template-columns: 1fr;
  }
  
  .signs-grid {
    grid-template-columns: 1fr;
  }
  
  .case-metrics {
    flex-direction: column;
    gap: 1rem;
  }
  
  .testimonial-author {
    flex-direction: column;
    gap: 1rem;
  }
  
  .audit-dashboard {
    padding: 1.5rem;
  }
  
  .dashboard-header {
    flex-direction: column;
    gap: 1rem;
  }
}

/********************
  Animations
*********************/
@media (prefers-reduced-motion: no-preference) {
  .feature-card,
  .coverage-card,
  .sign-card,
  .feature-item {
    animation: fadeInUp 0.6s ease-out;
  }
  
  .feature-card:nth-child(2) { animation-delay: 0.1s; }
  .feature-card:nth-child(3) { animation-delay: 0.2s; }
  
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', () => {
  const form = document.querySelector('form[name="seo-audit-tool"]');
  const input = document.getElementById('audit-website-url');
  const loadingState = document.getElementById('loading-state');
  const resultsContainer = document.getElementById('audit-results');
  
  if (!form || !input || !loadingState || !resultsContainer) {
    console.error('Required elements not found');
    return;
  }

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    
    const url = input.value.trim();
    if (!url) return;

    // Show loading state
    loadingState.classList.remove('hidden');
    resultsContainer.classList.add('hidden');
    
    // Simulate analysis delay
    await new Promise(resolve => setTimeout(resolve, 2000));
    
    try {
      const analysis = await analyzeWebsite(url);
      displayResults(analysis, resultsContainer);
      
      // Hide loading, show results
      loadingState.classList.add('hidden');
      resultsContainer.classList.remove('hidden');
      
      // Smooth scroll to results
      resultsContainer.scrollIntoView({ behavior: 'smooth', block: 'start' });
      
    } catch (error) {
      console.error('Analysis failed:', error);
      showError(resultsContainer);
      loadingState.classList.add('hidden');
      resultsContainer.classList.remove('hidden');
    }
  });

  async function analyzeWebsite(url) {
    // Simulate realistic SEO analysis with varying scores
    const baseScore = 60 + Math.random() * 30; // 60-90 range
    
    const scores = {
      technical: Math.max(50, Math.min(100, baseScore + (Math.random() - 0.5) * 20)),
      content: Math.max(40, Math.min(100, baseScore + (Math.random() - 0.5) * 25)),
      performance: Math.max(30, Math.min(100, baseScore + (Math.random() - 0.5) * 30)),
      mobile: Math.max(60, Math.min(100, baseScore + (Math.random() - 0.5) * 15))
    };
    
    const overallScore = Math.round((scores.technical + scores.content + scores.performance + scores.mobile) / 4);
    
    return {
      url,
      overallScore,
      scores: {
        technical: Math.round(scores.technical),
        content: Math.round(scores.content),
        performance: Math.round(scores.performance),
        mobile: Math.round(scores.mobile)
      },
      issues: generateIssues(overallScore),
      recommendations: generateRecommendations(overallScore)
    };
  }

  function generateIssues(score) {
    const allIssues = [
      { type: 'critical', text: 'Missing meta descriptions on key pages', priority: 'High' },
      { type: 'warning', text: 'Images missing alt text attributes', priority: 'Medium' },
      { type: 'info', text: 'Page loading speed could be improved', priority: 'Medium' },
      { type: 'warning', text: 'No schema markup detected', priority: 'Low' },
      { type: 'critical', text: 'Broken internal links found', priority: 'High' },
      { type: 'info', text: 'Mobile viewport not optimized', priority: 'Medium' },
      { type: 'warning', text: 'H1 tags missing on some pages', priority: 'High' },
      { type: 'info', text: 'SSL certificate needs renewal soon', priority: 'Low' }
    ];
    
    // Show more issues for lower scores
    const issueCount = score >= 80 ? 2 : score >= 60 ? 4 : 6;
    return allIssues.slice(0, issueCount);
  }

  function generateRecommendations(score) {
    const allRecommendations = [
      'Add compelling meta descriptions to all pages',
      'Optimize images with proper alt text and compression',
      'Improve page loading speed with caching',
      'Implement schema markup for better rich snippets',
      'Fix broken internal links',
      'Optimize for mobile devices',
      'Add proper H1 tags to all pages',
      'Update SSL certificate'
    ];
    
    const recCount = score >= 80 ? 3 : score >= 60 ? 5 : 7;
    return allRecommendations.slice(0, recCount);
  }

  function displayResults(analysis, container) {
    const getScoreColor = (score) => {
      if (score >= 80) return { text: 'text-green-600', bg: 'bg-green-100', border: 'border-green-200' };
      if (score >= 60) return { text: 'text-yellow-600', bg: 'bg-yellow-100', border: 'border-yellow-200' };
      return { text: 'text-red-600', bg: 'bg-red-100', border: 'border-red-200' };
    };

    const overallColors = getScoreColor(analysis.overallScore);
    
    container.innerHTML = `
      <div class="bg-gradient-to-br from-blue-50 to-white rounded-2xl p-8 border border-blue-100">
        <!-- Header -->
        <div class="text-center mb-8">
          <div class="inline-flex items-center gap-2 bg-blue-600 text-white px-4 py-2 rounded-full text-sm font-medium mb-4">
            <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
            </svg>
            Analysis Complete
          </div>
          <h3 class="text-2xl font-bold text-gray-900 mb-2">SEO Audit Results</h3>
          <p class="text-gray-600">${analysis.url}</p>
        </div>

        <!-- Overall Score -->
        <div class="text-center mb-8">
          <div class="inline-flex items-center justify-center w-24 h-24 ${overallColors.bg} ${overallColors.border} border-2 rounded-full mb-4">
            <span class="text-3xl font-bold ${overallColors.text}">${analysis.overallScore}</span>
          </div>
          <div class="text-lg font-semibold text-gray-900">Overall SEO Score</div>
          <div class="text-sm text-gray-600">
            ${analysis.overallScore >= 80 ? 'Excellent! Your site is well optimized.' : 
              analysis.overallScore >= 60 ? 'Good foundation with room for improvement.' : 
              'Significant optimization opportunities available.'}
          </div>
        </div>

        <!-- Score Breakdown -->
        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-4 mb-8">
          ${Object.entries(analysis.scores).map(([category, score]) => {
            const colors = getScoreColor(score);
            const categoryNames = {
              technical: 'Technical SEO',
              content: 'Content Quality', 
              performance: 'Performance',
              mobile: 'Mobile UX'
            };
            
            return `
              <div class="bg-white rounded-xl p-4 border border-gray-200 text-center">
                <div class="text-2xl font-bold ${colors.text} mb-1">${score}%</div>
                <div class="text-sm font-medium text-gray-900">${categoryNames[category]}</div>
              </div>
            `;
          }).join('')}
        </div>

        <!-- Issues & Recommendations -->
        <div class="grid md:grid-cols-2 gap-6">
          <!-- Issues -->
          <div class="bg-white rounded-xl p-6 border border-gray-200">
            <h4 class="text-lg font-semibold text-gray-900 mb-4 flex items-center gap-2">
              <svg class="w-5 h-5 text-red-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
              </svg>
              Issues Found
            </h4>
            <div class="space-y-3">
              ${analysis.issues.map(issue => `
                <div class="flex items-start gap-3 p-3 bg-gray-50 rounded-lg">
                  <div class="w-2 h-2 rounded-full mt-2 ${
                    issue.type === 'critical' ? 'bg-red-500' : 
                    issue.type === 'warning' ? 'bg-yellow-500' : 'bg-blue-500'
                  }"></div>
                  <div class="flex-1">
                    <div class="text-sm font-medium text-gray-900">${issue.text}</div>
                    <div class="text-xs text-gray-600">Priority: ${issue.priority}</div>
                  </div>
                </div>
              `).join('')}
            </div>
          </div>

          <!-- Recommendations -->
          <div class="bg-white rounded-xl p-6 border border-gray-200">
            <h4 class="text-lg font-semibold text-gray-900 mb-4 flex items-center gap-2">
              <svg class="w-5 h-5 text-green-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
              </svg>
              Recommendations
            </h4>
            <div class="space-y-3">
              ${analysis.recommendations.map(rec => `
                <div class="flex items-center gap-3 p-3 bg-green-50 rounded-lg">
                  <svg class="w-4 h-4 text-green-600 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                  </svg>
                  <span class="text-sm text-gray-900">${rec}</span>
                </div>
              `).join('')}
            </div>
          </div>
        </div>

        <!-- CTA -->
        <div class="text-center mt-8 pt-6 border-t border-gray-200">
          <p class="text-gray-600 mb-4">Want a detailed analysis and implementation plan?</p>
          <a href="/contact" class="inline-flex items-center gap-2 bg-gradient-to-r from-blue-600 to-purple-600 text-white px-6 py-3 rounded-full text-sm font-medium mb-4 shadow-2xl">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
            </svg>
            Get Professional SEO Help
          </a>
        </div>
      </div>
    `;
  }

  function showError(container) {
    container.innerHTML = `
      <div class="bg-red-50 border border-red-200 rounded-2xl p-8 text-center">
        <div class="w-16 h-16 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-4">
          <svg class="w-8 h-8 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
        </div>
        <h3 class="text-xl font-semibold text-gray-900 mb-2">Analysis Failed</h3>
        <p class="text-gray-600 mb-4">We couldn't analyze your website. Please check the URL and try again.</p>
        <button onclick="location.reload()" class="bg-red-600 text-white px-6 py-2 rounded-lg hover:bg-red-700 transition-colors">
          Try Again
        </button>
      </div>
    `;
  }
});
</script>

<!-- FAQ behavior -->
<script>
// Expose globally for inline onclick handlers in markup
  window.toggleFAQ = function(btn) {
    try {
      const item = btn.closest('.faq-item');
      if (!item) return;
      const answer = item.querySelector('.faq-answer');
      const icon = btn.querySelector('i');

      const isOpen = item.classList.contains('open');
      // Close any inline height to allow transition
      if (isOpen) {
        answer.style.maxHeight = answer.scrollHeight + 'px'; // set current height
        // force reflow before collapsing
        void answer.offsetHeight;
        answer.style.maxHeight = '0px';
        item.classList.remove('open');
        if (icon) { icon.classList.remove('fa-minus'); icon.classList.add('fa-plus'); }
        btn.setAttribute('aria-expanded', 'false');
        if (answer) answer.setAttribute('aria-hidden', 'true');
      } else {
        item.classList.add('open');
        // set to scrollHeight for smooth transition
        answer.style.maxHeight = answer.scrollHeight + 'px';
        if (icon) { icon.classList.remove('fa-plus'); icon.classList.add('fa-minus'); }
        btn.setAttribute('aria-expanded', 'true');
        if (answer) answer.setAttribute('aria-hidden', 'false');
      }
    } catch (e) { /* no-op */ }
  };

// Progressive enhancement: initialize ARIA and collapsed state
document.addEventListener('DOMContentLoaded', () => {
  document.querySelectorAll('.faq-item').forEach((item) => {
    const btn = item.querySelector('.faq-question');
    const answer = item.querySelector('.faq-answer');
    if (btn && answer) {
      // Ensure starting collapsed
      item.classList.remove('open');
      btn.setAttribute('aria-expanded', 'false');
      answer.setAttribute('aria-hidden', 'true');
      answer.style.maxHeight = '0px';

      // Bind click listener as a fallback to inline onclick
      btn.addEventListener('click', (e) => {
        e.preventDefault();
        window.toggleFAQ(btn);
      });
    }
  });
});
</script>
